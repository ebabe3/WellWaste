import {React} from 'react';
import {View, Text, StyleSheet, FlatList} from 'react-native';

const WarehouseList = () => {
  return (
    <View>
      <Text>WarehouseList</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default WarehouseList;
